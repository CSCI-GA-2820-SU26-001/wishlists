apiVersion: v1
kind: Service
metadata:
  name: wishlist
  labels:
    app: wishlist
spec:
  type: ClusterIP
  selector:
    app: wishlist
  ports:
    - name: http
      port: 8080
      targetPort: 8080