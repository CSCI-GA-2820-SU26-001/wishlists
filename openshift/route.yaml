apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: wishlist
spec:
  to:
    kind: Service
    name: wishlist
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
